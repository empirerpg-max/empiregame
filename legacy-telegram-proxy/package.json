{
  "name": "legacy-telegram-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "description": "Proxy de streaming de vídeos legados do Telegram via MTProto (GramJS), mesma lógica do empireuploadsfinal/server.ts. Roda como serviço Node standalone (fora do Cloudflare Workers) porque MTProto precisa de socket persistente, que o Workers não suporta.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "telegram": "^2.26.22",
    "big-integer": "^1.6.52",
    "dotenv": "^16.4.5"
  }
}
